import React from "react";
// nominations / NominationTimeline
// TODO: Build the NominationTimeline UI.

const NominationTimeline = () => {
  return (
    <section className="nominationtimeline">
      {/* NominationTimeline content goes here */}
    </section>
  );
};

export default NominationTimeline;
